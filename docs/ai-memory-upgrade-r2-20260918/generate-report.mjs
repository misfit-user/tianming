import fs from 'node:fs';
const dir = 'docs/ai-memory-upgrade-r2-20260918';
const v = JSON.parse(fs.readFileSync(dir + '/verification.json','utf8'));
if (!v.specialistComplete || !v.extendedComplete || !v.architectureComplete || !v.ownDeltaClean || v.specialist.fail || v.files.some(f=>!f.expectedHashMatches || f.syntaxExit)) throw Error('Verification is not complete');
const text = [
'# 天命 AI 记忆系统第二轮升级：实现与验收',
'日期：2026-09-18。设备：LAPTOP-AV4J1O7I。目录：`C:\\Users\\37814\\Desktop\\tianming`。',
'## 交付范围',
'本轮针对长期记忆、模型能力自适应、能力检测、自主整合及混合检索进行了本地代码升级。共涉及 ' + v.files.length + ' 个运行时、加载入口及测试文件，新增三个运行时模块：`tm-memory-long-term.js`、`tm-memory-adaptive.js`、`tm-memory-hybrid.js`。另新增三套专项测试与测试辅助文件。',
'分支仍为 `' + v.branch + '`，HEAD 仍为 `' + v.head + '`。没有提交、推送、安装依赖、修改版本、打包、部署或清空存档；没有覆盖其他窗口已有修改。',
'## 一、长期记忆：扩大保留范围，不盲目扩大提示词',
'保留原有活跃记忆集合，在其容量裁剪之前将合格记录收进独立长期档案。长期档案上限为 2048 条和 240 万序列化字符，先达到的上限生效；单条保留文本有 1600 字符上限。该数值是字符预算，不是 2.4MB 文件大小，也不是模型 token 上限。',
'档案统一支持十二类：事件经历、事实知识、人物经历、关系变化、承诺义务、决策依据、因果经验、制度沿革、地域变迁、经济变化、未解线索、纠错记录。这些分类包含原有类型与新增类型，不表示十二类全部是从零新增。',
'已接入已接受记忆裁剪前保留、回合归档/rollup 收集和主推演 `long_term_memory_updates` 候选写回。候选必须引用当前可核对的记忆 ID 或来源引用；未知引用进入隔离。自行概括的结论仍走草稿审核，不能自行升级为引擎真值。只有满足既有自动接受开关、公开范围、置信度以及可核对原文等条件的低风险条目，才可能自动接受。',
'容量裁剪优先保护置顶等高价值记录，并记录裁剪数量。纠错、隐藏和替代标记随长期档案保留，防止短期控制表裁剪后旧记录重新进入召回；显式清除控制支持恢复。仍遵守既有世界、回合、角色权限和时间线边界。有限档案并非无限记忆，也不能恢复原来已经被删除的历史原文。',
'## 二、按模型能力自适应',
'窗口容量和记忆处理能力分开使用。能力证据按主/次 API、端点和模型标识区分，并设七天有效期；切换配置、过期或证据缺失时不会继续冒用原来的深度档。长期存储容量不随模型降档而缩小，改变的是单次读取和整合策略。',
'| 策略 | 默认记忆块上限 | 单次候选量 | 自主检索最大深度 | 整合输出预算 |\n|---|---:|---:|---|---:|\n| 精简辅助 | 1200 tokens | 6 条 | 有相应证据时最多 1 轮、2 次工具 | 1200 tokens |\n| 均衡记忆 | 3000 tokens | 12 条 | 有相应证据时最多 1 轮、4 次工具 | 3000 tokens |\n| 深度记忆 | 6000 tokens | 20 条 | 有相应证据时最多 2 轮、6 次工具 | 6000 tokens |',
'表中是默认上界。记忆块还受窗口比例预算、手动配置和最终请求总预算约束；小窗口会低于表值。手动零预算保持为零，不被默认值重新开启。模型名称大、宣称窗口大，不会单独构成启用深度自主检索的依据。',
'执行路径分为三种：原生工具调用；对记忆处理能力已验证、但原生工具不可用的模型使用严格 JSON 检索计划；未验证或不适合自主规划的模型使用本地混合检索。主/次 API 的能力配置分别保留。',
'## 三、模型能力检测',
'证据校验从原六项扩展至九项。新增：随机化的中段证据与新旧冲突检索；履约、未决和传闻分类整合；真实原生工具调用及参数检查。随机任务的预期答案由本地校验器产生，不以模型声称“会记忆”作为通过标准。',
'同时收紧严格 JSON 验证、数组内容核对和重复输出计数。超时、HTTP 错误等归为未测定，不直接当成推理能力失败；全部不可用时显示未知可靠性。探测结果写回前检查配置身份和任务顺序，旧请求不得覆盖新配置或更新的探测。',
'通常九次基础请求，失败时可能产生有界重试。这些是用于记忆机制选择的小型能力样例，不是模型完整上下文窗口的压力验收，也不能证明转发商的真实底层模型身份。本轮开发没有运行用户真实付费 API 的九项检测；测试采用本地模拟响应。',
];
text.push('## 四、自主整合与混合召回');
text.push('检索结合中文词组匹配、BM25 式词项评分、向量结果的倒数排名融合和去重/多样性筛选。增加 `read_memory` 展开证据，以及 `recall_related` 沿关系边查找关联记录。经过验证的模型可在第一次查找后继续补查，而不是只有一次调用；重复查询与总调用次数受限制，切档或取消后旧结果被丢弃。没有向量模型或索引未补齐时，仍可使用本地词项检索。');
text.push('长期记忆已纳入向量索引。文本采用重叠分块，正文变化会更新向量，旧版本在同一持久化事务中替换；持久化失败保留上一个完整索引。查询先检查当前来源、权限与正文有效性，再进入 top-K，避免私有/已撤销/已变更的旧向量占满结果。查询嵌入使用最多 32 项缓存；新增长期记忆每轮默认最多补建 256 个向量片段，余项留待后续调用继续处理。');
text.push('记忆管家采用模型对应的任务数、输入量与输出预算；同一世界的整合保持单实例在途。返回缺失必要层、源记录已改变、世界/模型配置变化或操作取消时，不清除原始记忆。压缩仅移除本次已处理的旧前缀，期间新追加记录继续保留。自由概括仍可能存在误差，因此保留原始证据和低权威定位，不将摘要变成引擎事实。');
text.push('## 五、验证结果');
text.push('| 检查 | 结果 |\n|---|---|\n| 本轮修改前记忆/上下文/模型/语义专项 | ' + v.baselineTests.pass + '/' + v.baselineTests.selected + ' 通过 |\n| 最终同范围专项，含三套新增测试 | ' + v.specialist.pass + '/' + v.specialist.selected + ' 通过，0 豁免、0 重试掩盖 |\n| 三套新增测试内部场景 | 41 组通过（28 组适配与记忆、9 组向量、4 组检测） |\n| 扩展回归：回合、存档、schema、启动与 agent | ' + v.extended.pass + '/' + v.extended.selected + ' 通过，' + v.extended.fail + ' 项失败 |\n| 本轮相对开工备份的差异检查 | 通过 |\n| 本轮文件语法和哈希校验 | 通过 |');
text.push('新增用例包括：150 条已接受记忆在活跃集合裁剪至 80 条后仍进入长期档案；2048 条/字符预算；类型与来源保留；原生工具与 JSON 两条规划路径；角色隐私；治理标记裁剪与撤销；原子向量替换失败；查询缓存；整合并发追加与切档；以及直接执行既有 IDB/project 快照构造函数，确认长期档案字段被保留且脱离 live 对象。快照测试采用 prepare:false，不是完整真实客户端读档验收。');
text.push('检测测试调用了真实检测函数，但网络响应全部由本地测试桩提供；九项探测是否在你的实际供应商上通过，仍须在 API 面板执行。这里没有将模拟响应结果当作真实大模型准确率、速度或费用提升。');
text.push('架构检查仍有以下失败类别：`' + v.architectureFailures.join('`、`') + '`。与上一轮验收记录的失败类别相同；这不表示整仓全绿，也不是逐条诊断文字完全相同的证明。本轮未重跑整仓全部 1038 项 smoke，扩展回归是按主题选择的 ' + v.extended.selected + ' 项。');
text.push('扩展回归失败脚本：\n\n' + v.extendedFailures.map(row => '- `' + row.name + '`（退出码 ' + row.exit + '）').join('\n'));
text.push('对 HEAD 的整份工作区差异检查仍报告共享 schema/validator 文件的行尾警告；本轮新增差异已按开工备份单独核对清理，未顺手改写其他窗口已有行。原始警告、备份比对与完整测试输出全部保留。由于缺少本轮修改前的扩展回归基线，不能把其中每项失败都宣称为已独立证明的历史问题。');
text.push('## 六、使用与交付边界');
text.push('本地源码重新加载后，API 检测面板会显示记忆策略、规划路径以及长期档案数量。主、次 API 应分别执行证据校验。自主召回和记忆管家仍遵守既有独立开关与模式互斥规则，没有替用户开启额外收费选项，也没有更改实验 Agent 模式与普通主推演模式的切换约定。');
text.push('未进行真实付费 API 长局、手机端或 Electron 客户端完整验收；没有修改已部署网页。长期记忆容量有限，模型每轮只能读取其中一部分，也不保证模型必然遵从每条证据。');
text.push('## 七、文件与证据');
text.push(v.files.map(f => '- `' + f.file + '`').join('\n'));
text.push('证据目录：`docs/ai-memory-upgrade-r2-20260918/`。关键记录：`baseline.json`、`changes.json`、`baseline-tests.json`、`final-specialist-tests.json`、`extended-tests.json`、`final-architecture.log`、`own-diff-check.json`、`verification.json`。');
text.push('修改前备份：`.bak-ai-memory-upgrade-r2-20260918/`。恢复前核对当前文件哈希，并逐文件合并，避免覆盖其他窗口后续修改。');
fs.writeFileSync(dir + '/README.md', text.join('\n\n') + '\n', 'utf8');
console.log('Report written: ' + dir + '/README.md');
