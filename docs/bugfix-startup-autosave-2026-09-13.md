# 取消桌面启动恢复询问（本地验收）

基于已部署的 `80a20bf9`，分支 `codex/fix-startup-autosave-prompt`。本次未推送、合并或发布，也未操作玩家存档/已安装客户端。

## 行为

- 桌面启动不再因为自动存档包含 `gameState.running` 就弹出系统确认框，也不会擅自进入上一局。
- 60秒定时自动保存不变，已有备份不删除。旧的仅含项目数据的兼容恢复保留。
- 读取存档/卷宗目录新增“桌面自动存档”，用户主动点选并确认后，通过原桌面读档事务恢复；使用主进程已校验并解包的自动存档及原 session token。失败保留读档界面和备份。
- 恢复验收发现旧清理逻辑把官方人物“安邦彦”的前缀“安邦”当作垃圾词。现在保护当前剧本明确声明的稳定 ID + 名字，不放开其他误抓词或其他剧本的实体，官方 JSON 不改。

## 验证

- 新行为测试：9 PASS。包含 T1/T25 不弹窗、不自动载入，项目兼容恢复，读不到备份不阻塞启动，60秒计时器保留，手动恢复 token，失败不假成功，以及主动确认边界。
- 原生验收：9 PASS。真实 Electron 主进程/preload/IPC；先创建实际 T1 自动存档，再连续重载两次，菜单保持可用、备份字节不变；经可见且可点击的真实读档按钮恢复，全部203个稳定人物 ID 保留。
- 全量：953 PASS / 0 FAIL / 0 SKIP / 0 WAIVED；架构13项、官方派生27条、完整发布契约及1102项基线检查通过，资源无删除，版本字段不改。
- 原生证据：`web/dev-tools/electron-bridge/a11c1e90-b707-4227-a876-16fcf2344796/report.json`；同目录保留启动与手动恢复界面截图。入口须实际命中且祖先透明度完成过渡，再做恢复与全部身份检查。
- 全量证据：`web/dev-tools/arch-guard/ci-Ds8vu4/smoke-report.json`。历次失败与命令退出码保留在 `web/dev-tools/office-writeback/startup-*`。

## 失败修正记录

初始反例3通过/3失败；原生恢复先报203→202，逐项ID比对及原档核验排除了去重假设，定位安邦彦被词表误删。修复后全部身份保留。新增独立读档实现触发行数闸后改为复用既有事务、UI别名归UI模块；没有压缩行或提高阈值。按钮命中检查曾被启动邸报遮挡，测试按实际“已阅·闭卷”流程关闭该浮层，不删除命中断言或改动生产邸报。启动清单在提供者索引更新后重新生成，完整套件随后全绿。
