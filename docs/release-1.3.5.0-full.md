# 1.3.5.0 全量测试安装包

本次仅发布全量 Windows EXE、Android APK 与网页源码；不生成或发布 1.3.5.0 的桌面 / Capgo 热更新，不修改 OTA 指针。

- 整合本机日常目录、地图、六项 UI、原生开局工作台及晚唐 v29 的已完成内容，保留最新远端改动；原工作目录不覆盖。
- 版本：内容 1.3.5.0、Electron 包 1.3.500、Android versionCode 1352。
- 晚唐 v29 真源只移除 JSON 字符串外空白以满足 GitHub 单文件限制，数据深比较一致；三个官方剧本派生物已同步。
- 运行资源 1,287 项，包含晚唐立绘 126 项、preview/img 41 项、本地语义模型和配套 WASM；与 1.3.4.11 旧包对照，没有未解释的运行文件缺失。
- 排除旧设计稿、截图、开发工具、备份、临时地图、密钥与玩家存档；没有整目录排除 preview。
- 仓主明确同意 Windows 未签名测试包，可能提示未知发布者。使用 `node scripts/build-electron.js --platform win --unsigned-test`；默认正式构建仍强制签名。测试包不能冒充已签名的自动更新安装包。
- APK 沿用既有 Android 测试签名，不改变 applicationId，不清空玩家数据。

验收范围：架构 13/13、官方数据对账 39 项、发布契约通过；真实 Electron 晚唐开局和原生核心、离线语义推理通过。全量 smoke 首轮 1,015/1,024，通过修复后原 9 个失败项全部定向通过。按仓主后续指示停止第二轮全量复跑，不宣称第二轮全量已通过。没有调用真实模型 API。

最终安装器大小、SHA-256 与发布状态以产物目录 `E:/版本/测试版1.3.5.0` 和对应 GitHub Release 为准。
