# 地图缩放预加载与文字清晰度

已同步到 `C:/Users/37814/Desktop/tianming` 的本地源码版，重启后生效。保留既有地图分层/疆域排字及 C 目录其他修改；未改剧本、玩家存档、API、版本或部署。原生工作台分支未动。

## 本次修改

- 首次加载地图时一次准备天下、省道、府州的边界、排版和实际字体尺寸，显示准备进度；每个世界最多保留三份图层。切换剧本/地图内容变化后重新准备，不复用旧世界。
- 几何排版在固定本地 Worker 处理；不可用时逐项让出事件循环。点包含判断使用精确边段索引，保留原有多边形/孔洞及实际字形检查。
- 滚轮按滚动量连续缩放，同帧输入合并。缩放切换已准备图层，移动中复用几何画面，结束后恢复精细边线。
- 文字从缩放相机分离为独立 SVG 矢量层，保持坐标对齐，减轻描边并提高对比；保持原有字号下限与按疆域形状排版。未删地块/顶点或降低地图分辨率。

## 简单检查与范围

- 索引与穷举一致、缓存失效/取消、三层复用、输入合并：5 组通过。
- 投影/字体/碰撞：23 项通过；本地 C 目录的实际渲染路径：16 项通过。
- 架构守卫 13/13 通过；未追加全量 smoke 或发版验收。
- 明朝原件的实际窗口检查确认 `sc-tianqi7-1627 / 1627 / tianqi-ming2` 配对正确；三层均有 43 地块，文字层独立且偏差小于 1 像素，真实滚轮生效。记录：`web/dev-tools/electron-bridge/f04d538a-7f20-4b8d-a1dd-c04d1afe3f4d/`。
- 上述窗口的功能断言通过，但测试进程退出被外层超时终止，所以整份 runner **不是 PASS**。本轮遵用户要求停止扩大检查，没有把旧性能记录或退出失败包装为整套验收通过。
- 同一份 575 地块、733 项离线排版样本，耗时从约 40.6 秒降到 10.9 秒；这是单机排版计算对比，不是整局载入时间或帧率保证。当前版本未通过跨设备完整性能验收。

## 测试纠正与保护

早期为了隔离地图开销曾用明朝模板配晚唐地图，造成用户看到混合年代。这是本轮测试构造错误，已经撤回；没有写入正式存档，也不能拿它判定生产开局串图。后续使用原剧本配对检查，另以缓存换世界回归防止旧图层复用。原始失败记录保留。

内嵌底图转 PNG 的试验未证明有效，已撤回，未保留额外栅格缓存。没有进一步调用真实 API，也没有重跑全量 CI 或发版检查。

本地同步按函数逐项匹配施工前源码，七个文件只落本次补丁。备份：`E:/tianming-office-writeback/_codex_tmp/zoom-local-20260915/`。当前资源基线尚未为本次改动更新，不能直接当作已发布版本。
