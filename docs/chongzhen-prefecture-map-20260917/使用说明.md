# 崇祯开局：继承省道轮廓的三级地图

## 实际入口

官方源仍为 `scenarios/天启七年·九月（官方）.json`，ID仍为 `sc-tianqi7-1627`。
这是朱由检即位的原开局，未擅自修改日期、改元配置、故事、事件或角色能力。
重新启动本地开发项目，在官方剧本中新开该剧本，即可使用新图。无需另行导入。
已经运行的旧局不会被强制迁图；本轮没有打开或覆盖用户存档。
本次没有执行 Git 提交、推送、打包、热更新或网页发布。

## 三层地图

- 天下：原生按当前势力聚合，不新增国家账户。
- 省道：43个原有省道／地域轮廓的并集视图。
- 府州：284个可操作逻辑单元，包含270个细分单元和14个保留粗区。

29个父区已经细分。14个父区保留原粒度：其中13个有明显的原轮廓与地点参照冲突，澳门因范围过小不拆。
例如土默特的原图位置与归化城参照明显不合；关宁东江原轮廓也不能准确覆盖原名册所有驻点。
不把真实城名硬挪到错误省界里，也不把这些保留粗区称为已经精绘的府州。
本轮保留原父区的边界、领属及已有交叠，不进行原图政治地理重置。

## 账户与人物

382份原基层账户全部保留：359份绑定图内地块，23份原本属于流动群体／海商／叛乱等的账目仍保持原状。
细分没有新增人口、钱粮、皇产、征兵额。地块显示与核算账户分开，不把省级汇总再算一遍。
203名原人物、48支原部队、62条原事件及原官职、关系、国库、内帑字段保留。
人物和军队新增地图定位；原所在地文字不改写。范围描述和多点部署不伪造成唯一营地。
开局实际生成的运行时部队数量可不同于48支原始部队，属于原引擎初始化行为；本轮未增加原始部队记录。

## 易主和运行适配

单府易主只迁移对应原账，不带走同省其余府州。
整省命令先展开为具体地块，批次统一校验；收复归还原省级容器。
空省级汇总项进入专用归档，不变成一份新增人口；批次失败时回滚完整容器和账户结构。
已补上原生页面对既有公共财政服务、既有所在地服务的加载入口。

## 制作数据

`web/data/maps/chongzhen-prefecture-v1/geometry.json`：不携带人物、初始控制方、财政账的几何与分组数据。
`web/data/maps/chongzhen-prefecture-v1/scenario-bindings.json`：对应剧本的账户、归属和所在地关联。
完整运行数据同时打入原官方JSON，由 `web/scripts/sync-official-scenarios.js` 派生到内置与编辑器数据。
不要从派生JS反向覆盖官方源。不要拿原43区的制图工具直接重新覆盖本版，否则会退回省级单层。

作者脚本、地点表和概化山系在本目录；大体积冻结输入、候选、备份和原始报告在：
`D:/tianming-assistant-work/chongzhen-prefecture-map-20260917/`。
原件为 `source-before.json`；地图应用备份为 `applied-backup/`；运行时备份为 `membership-applied-backup.js`。
回滚前必须核对当前摘要及后续修改，禁止把这些备份直接覆盖其他窗口新增的工作。

## 精度和验收边界

府州边界采用治所参照和概化山系约束推定，非1627年测绘疆界；生成后在每个父区内共享边界并检验并集。
同一岛屿组／多面地块共用逻辑ID与原账，不生成“分片1”账户。
原图中既有的疆域、年代地名和跨父区交叠问题没有被本轮暗中重新考定。
三级截图使用真实本地网页程序的新局；测试将层级与缩放暂时分开，三图使用同一全景相机。
截图不是独立审图器，也不是用户真实存档；运行在隔离Electron数据目录，无联网AI。
已检验专项几何、账目保全、单府和整省易主、收复、失败回滚、府州定位及三级画面。
完整长局、联网AI、手机性能和旧存档迁移未验收。全仓架构门禁还有未通过项，见 `验收结果.json`，不视作正式发布通过。
