# 晚唐官方剧本接入

已接入本机项目的官方剧本列表：晚唐·开成五年——山海诸邦（sc-tang840-840）。默认唐廷开局；488人、575地区、24个玩家阶层、126张立绘。原v18完整交付和47势力选择器保留不变。

改动包括官方JSON真源、11个同步产物中的必要更新、桌面登记/补种、9个既有兼容文件、缓存戳和启动清单。23个写回文件均有字节校验备份：D:/Codex-Verification/tang840-official-install-backup。原有其余工作区文件哈希核对未变。

## 实际验收

- 本机官方同步：3源、11派生物；对账39项通过。
- 从本机web目录打开真实首页，点击晚唐卡片、采用剧本地图、命名、新开局：通过。未使用测试注入的剧本或引擎覆盖。
- 官方大文件仅在开局时加载；488人均有立绘，126图逐张解码；实际存档及读取通过，浏览器无未捕获异常。
- 实际地图575区、唐廷24阶层；事件执行与财政入库接口在运行时可用。
- 桌面登记与仅补缺失剧本的补种逻辑通过。
- 隔离副本架构13/13通过。本机12/13通过，ref-check扫到原有backups/scenario-origin-.../index.html的缺席脚本；未删除用户备份或放宽守卫。
- 全量smoke：945/947通过。smoke-production-dependencies和smoke-relief-governance在未接入前的原目录均复现同一失败，非晚唐新增。
- 发版契约未通过：原工作区资源已超旧950MiB预算，本次接入后的资源约1.58GB；热更基线未更新。未提高包体预算、未改版本号/热更基线、未打包/推送/发布。

重启从此项目启动的天命后，进入「开卷」选择晚唐即可；已有存档不会转换。此结果是本机官方入口接入，不代表线上客户端已经更新。
