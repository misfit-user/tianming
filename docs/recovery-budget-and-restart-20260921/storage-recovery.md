# 2026-09-21 本地存储恢复与固定复核预算

后续规则已于 2026-09-22 扩大为整条链路非阻塞完成，见 [主推演完成后正常过回合](nonblocking-completion.md)。本页保留存储恢复及预算问题的实证。

## 实际原因和数据核验

原本 19:58 启动的 Electron 主进程在窗口关闭后仍被本次排查留下的 Node 调试连接占用，持有原用户目录的数据库锁。后续游戏实例读取 IndexedDB 报 `UnknownError: Internal error`，随后错误地走了空白 localStorage 降级路径；回合前的强制存档因此无法提交。这次调试连接干扰由助手引入。

关闭该调试连接后，旧 Electron 进程全部退出，IndexedDB 和 Local Storage 锁均释放。未删除或重建原数据库、未覆盖用户的当前自动档。

- 原目录：`C:/Users/37814/AppData/Roaming/tianming`。
- 已校验备份：`D:/Codex-Rescue/tianming-storage-20260921/profile-before-recovery`，149 个数据文件，共 352351571 字节，逐文件 SHA-256 与复制时原件一致；浏览器缓存、下载包和锁文件不在备份内。
- 在独立备份副本、随后在原目录进行只读检查：12 条游戏存档及 1 条编辑记录均能读取；包括 gzip 正文解压、JSON 解析。原设备 Key 存在，轻量配置包含 96 个字段，项目存储存在。
- 核验只输出存在性、记录 ID、回合及可读性，没有将 Key 内容写入报告。
- 用户重新打开原游戏后确认恢复正常；23:02 实际启动日志也显示 IndexedDB v6 就绪，并从 localStorage、IndexedDB 和桌面自动档恢复。

## 存储修复

- `main.js` 在载入会访问资料目录的实现代码前获取单实例锁。重复启动聚焦原窗口，不再同时打开同一数据库。
- 关闭保存握手通过、进入 `will-quit` 后关闭 Node inspector，避免调试连接留下无窗口进程。
- 桌面 IndexedDB 打不开时明确报错，存档管理器不显示伪造的空白目录；网页原有 localStorage 降级行为保留。
- 配置持久化、Key 设置与桌面自动保存受存储就绪状态保护。API 写入异常或读回不一致时不得显示已保存。
- 存储失败后即使重新建立读取连接，也必须重新载入旧资料才能恢复写入，避免启动默认值覆盖原配置。

## 23:04 真实过回合的后续失败

同一次正常重开的用户测试中，回合前存档成功，prep、plan-prefetch 完成，sc1/1b/1c 返回；23:10 回合在固定复核入口失败：首轮完整材料预计 93417 Token，原配置的应急修补预算为 48000 Token。固定复核不应复用仅为局部失败修补设计的预算。

已新增独立 `reviewMaxTokens`：默认 `0` 不设 Token 上限，正整数表示本次固定复核的累计 Token 上限。原 `maxTokens` 仍用于失败后的应急修补；已有玩家配置原值保留。调用、候选、工具和等待预算继续按玩家设置执行。设置界面和进度面板显示两类预算及“不设上限”。完整时政记、实录、其他生成结果、修改后真实读回和原文覆盖要求均未削弱。

增加 `[TurnReview]` 日志，记录阶段、调用数、估算 Token、有效上限和已读来源数；预算等错误形成时立即记录，而非等其他推演完成才显示。

该预算补丁于本地 23:15 写回源码；23:01 启动的游戏须完整重开才能载入。写回时未重载或关闭用户正在操作的游戏。

## 验证与边界

- 本地正式工作目录执行 25 个相关 smoke：25 PASS、0 FAIL。报告在 `D:/Codex-Rescue/tianming-storage-20260921/final-main-tests.json`。
- 新增存储故障保护 43 个断言、主进程所有权 7 个断言。原文复核回归新增 93417 Token 首轮材料、旧 48000 应急预算隔离，以及玩家明确设置有限复核预算时调用前拒绝的用例。
- 真实 Electron、D 盘独立测试资料：重复实例被拦截；故意保持调试 WebSocket 连接后退出，主进程和连接均结束；重新打开同一测试资料，模拟 Key、配置和 IndexedDB 存档保持一致。测试未访问用户资料。
- 原生准备清单重新生成并通过字节一致性检查，未改版本、热更基线、发布或安装包。
- 全量 1084 项测试曾启动，但并发期间出现 Windows 页面文件不足，已停止，不记为通过。定向测试以单并发重新执行。
- 架构检查的既有 `editor.html → TM.NativeWorld ← tm-native-fiscal-adapter.js` 缺失依赖另行记录；没有放宽守卫基线。
- 存储恢复已经由真实数据和用户重开确认；新的复核预算补丁仍须重开后的真实 API 回合验收，不能把模型桩回归当作完整实机过回合成功。
