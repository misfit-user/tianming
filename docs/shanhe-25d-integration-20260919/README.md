> 当前本地适配器：B5-native-2（R2），2026-09-20已完成地图回归与实际入口检查。最新记录见 [R2-closeout-20260920.md](R2-closeout-20260920.md)。以下保留早期接入过程，不能代替最新核验。

# 山河境 2.5D：本地实测版

已接入当前工程，不是独立审核页。双击项目根目录的「启动天命-山河境实测.cmd」，进入原游戏后照常选择剧本或读取存档。该入口直接使用本地 web，不运行 Git 同步；使用正常游戏的数据位置。

## 使用

原有拖拽、滚轮、地块档案、悬停提示和天下/省道/府州继续使用。舆图工具里新增「底图：山河境 2.5D / 原版」切换和「导出底图诊断」。切换只改变显示，不修改地块归属或存档。开关目前仅作用于当前页面。

底图不烧录文字，屏上地名仍属于原游戏的动态标签。四张 B5 纹理、环境与模型/光照代码保持审核版内容，没有通过删细节降画质收尾。

## 实际验收

当前明末307块、晚唐575块、绍宋566块均在本机 Electron 加载真实 web/index.html 出图。91项原生检查通过，覆盖三级地图、七种数据模式、点击原档案、滚轮锚点、约288像素拖动、回退/重启显示、资源复用及几何失效刷新。明末额外验证单地块易主与收复后显示颜色同步。23项坐标/素材/源码检查通过；三组既有地图回归与作用域内 git diff --check 通过。

实际GL渲染器为Intel UHD / ANGLE Direct3D11；测试隔离存储、阻止外部网络，无真实存档读取或付费AI调用。测试有4GB堆上限参数，未修改正常游戏启动参数。验收是接入正确性检查，不是60帧、GPU耗时或长局性能认证。诊断中的updateMs是CPU配置、绘制提交与标签处理时间，不能换算FPS。

## 数据与限制

使用正在运行的三个官方地图，不再加载旧审核几何。明末用224条现有控制记录核对变换；晚唐保留11个与地理参考不同的已调整显示中心，没有擅自移动地块；绍宋按其声明的X/Y比例转换，未另称已实测地理精度。美术高度不进入AI与游戏推演。

本轮没有改人物、财政、过回合逻辑，没有Git提交、推送、打包或部署。其他窗口的未提交改动保留。原有加载、首次准备和跨缓存重建仍需真实游玩观察。浏览器前进/后退缓存恢复、手机和长局未单独验收。

## 文件与证据

运行接入：web/tm-shanhe-runtime.js；资源：web/vendor/shanhe25d；原地图与入口只加有限适配钩子。原许可保存在资源目录的licenses。测试脚本在本目录，完整原生记录和截图在：

D:\tianming-assistant-work\shanhe-25d-20260919\delivery

前一轮因C盘空间耗尽产生的空记录不计入通过数。本轮只把自动测试自己生成的旧隔离缓存转存D盘，未清理用户其他文件；后续自动测试数据和主要证据落在D盘。正常游戏保存仍需要系统盘/原存档位置有可用空间。

实测卡顿时，导出底图诊断并注明剧本、地图层级和当时动作，用于区分镜头重建、地表绘制、标签和原游戏刷新成本。

## 2026-09-19 启动入口修复

已复现旧 CMD 将中文行尾截成命令、未走到 npm 的问题。入口现使用 ASCII 命令与 CRLF 换行，异常退出会停留显示错误码。请保持该启动脚本的原始字节格式，不用文本工具统一换行。文件名与启动位置未变，B5 渲染代码、美术资源未修改。本次实际入口在隔离目录加载真实 main.js/preload，并在可见窗口验证307块地图和底图开关。证据：D:\tianming-assistant-work\shanhe-b5-launch-repair-20260919\repair-summary.json。
