# 天命 · 山河境本地接入 R2：完成核验

核验时间：2026-09-20T04:15:41.503Z。游戏入口1.3.5.1，适配器B5-native-2，底图美术仍为已认可的B5。B6未安装。

## 使用

重新启动项目根目录的“启动天命-山河境实测.cmd”。舆图工具来源行应显示“本地1.3.5.1 · B5接入R2”和当前地块数。保留原版/2.5D切换以及诊断导出。正常启动使用玩家正常数据位置；本次自动检查使用D盘独立临时数据，不读取真实存档。

## 修复

入口曾残留1.3.4.11标注；现与已存在的1.3.5.1包信息一致。启动器先从当前本地代码生成原生准备清单，不执行Git同步、不安装依赖，也不覆盖其他窗口的修改。版本说明不等于把工作区整体重置为某个远端提交。

标签不再继承0.18秒的transform过渡，SVG与底图画布使用一致的显示尺寸。势力层读取原地图实际fill和fill-opacity，不再由适配器自行降至25%透明度，也不额外给势力色施加地形光照。悬停与选中使用独立贴地高亮，点击仍调用原方志和势力逻辑。

## 本次证据

|剧本ID|地块|通过检查|标签取样组|最大屏幕矩阵误差（CSS像素）|
|---|---:|---:|---:|---:|
|sc-tianqi7-1627|307|35|15|0.000189|
|sc-tang840-840|575|33|15|0.000175|
|sc-jianyan1-1127-shaosong|566|33|15|0.000193|

三个剧本独立启动、独立退出，均完成回归。每组标签测试有实际可见标签，不把空标签集合的零误差当作通过。检查还包括真实悬停和点击、清除旧选择后重新选中、配色与原CSS逐项核对、缩放保留选择、切回原版及再启用。明末额外验证地块易主、收复会自动更新底图颜色。

连续滚轮与216像素拖拽、三档窗口尺寸变化、闲置不重复重建在独立运动测试中核验。真实main.js和preload在可见隔离窗口另行检查入口版本、地图与开关。不是只运行独立底图HTML。

## 限定

屏幕误差指标签实际SVG矩阵相对本帧地表投影的误差，不是地理测量精度。测试不认证稳定60帧或长期游玩表现，不包含真实存档的长期回合。美术高度只用于显示。早先中断、超时及测试程序IO故障保留在原目录，不计入本次完成数。

本次未提交、推送、打包或部署；剧本源文件与加载脚本指纹已核对。其余窗口的代码保留，没有用旧审核场景替换本地地图。

原始记录：D:\tianming-assistant-work\shanhe-r2-closeout-20260920。核验摘要：D:\tianming-assistant-work\shanhe-r2-closeout-20260920/delivery/summary.json。
