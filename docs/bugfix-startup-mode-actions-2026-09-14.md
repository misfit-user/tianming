# 晚唐模式选择页没有后续操作：本地修复

## 定位

实际目标是本地 v21《晚唐·开成五年——山海诸邦》，不是旧 v16 候选，也不是尚未发布的大地图。晚唐新增的模式说明更长，原桌面模式页没有独立的内容滚动区：整个面板受 `main-view` 的 `80vh` 裁切，开始／返回按钮被排到了可视范围外。

真实旧版截图与坐标：在 `1100×800` 窗口，可视底边为 `640px`；普通模式的开始按钮位于 `758–795px`，展开严格史实参考文本后位于 `1089–1126px`。控制台没有脚本异常，按钮 DOM 与事件也没有丢失。较短的说明或更高的窗口会让问题不那么明显。

## 修改范围

- 模式页采用固定可用高度的纵向布局；标题、开始／返回不收缩，模式说明与参考文本在中间独立滚动。
- 保留原黑金样式、全部模式文案、史实规则及原点击处理，不改晚唐剧情或数据来迁就界面。
- 本地游戏 `C:/Users/37814/Desktop/tianming` 与待提交代码 `E:/tianming-office-writeback` 均修正 `web/tm-electron.js`；`web/index.html` 只更新该脚本缓存标识。
- 两边既有测试仅补齐简化 DOM 的 `firstElementChild` 属性；新增隔离回归和真实菜单验收在 E 工作树。
- 原件备份：`E:/tianming-office-writeback/web/backups/startup-mode-153ff07f-9c00-4748-af40-d92f62a21086`。未修改 D 盘集成树、API 设置或玩家存档。

晚唐 JSON 修前／修后均为 **51,935,308 字节**，SHA-256：

`32354b04aa1bb7d0a9c1054caf54dfb60d75fdf2b99c2320903b0fecfc28899f`

## 验收证据

从真实主菜单逐步点击选剧本、命名、选模式、开始、略过逐字、提笔临朝；没有用 `doActualStart` 直接跳过故障页。测试使用一次性配置目录，外网请求被阻断。

- 晚唐 v21：三个模式，`1100×800` 与 `1100×640`，开始按钮始终可点击；完整流程进入 T1，434 人物、`strict_hist`、输入的参考文本以及 `player-driven` 规则全部保留，0 JavaScript 异常。
- 正常天启：同样的完整入口与 12 项真实检查通过。
- C／E 的真实函数各 14 项新布局与动作断言通过；既有返回流程 13 项断言保留并通过。
- E 架构 13/13、全量 **968/968** 通过，0 失败／跳过／豁免／可疑退出。报告 `web/dev-tools/arch-guard/ci-kJxPro/smoke-report.json`，回执 `startup-mode-full-suite-2f5169e1-50d9-41bd-8d84-a464ceb25f52`；官方对账 27、发布契约 166、1118 项资源清单复查通过，游戏资产无删减。

原始报告位于 `E:/tianming-office-writeback/web/dev-tools/electron-bridge/`：

- `381d346f-9080-4991-b076-e1dc9bfd7746`：晚唐旧版缺按钮截图／坐标。
- `07c0dfdd-0866-49a5-9db8-8b06c14d8806`：晚唐修后完整真实开局与截图。
- `7887c655-fce1-41c5-8bbc-acf51bc6e5fe`：正常天启完整真实开局。

## 边界与使用

重新启动本地游戏后，新模式页才会加载。已经打开的旧页面不会被自动替换；没有强行关闭玩家窗口。

本次未提交、推送、合并或部署。C 开发目录整体另有原有的混合代码门禁问题（10/13）：缺少 AIOptions／APISettings／OfficeCreation 及 battle-contract 装载，还有历史备份页的引用检查；从修前备份对比可确认本次没有新增／删除任何装载点。它们未混入此项修复，也不把整个 C 目录宣称为可发布状态。晚唐本轮未做联网 AI 或连续回合验收。
