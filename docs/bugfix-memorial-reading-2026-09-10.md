# 奏疏列表与阅读设置修复

## 交付范围

起点：`main@547e2bb14bd4a61cbe5927b2ca7c2c8f55859ff8`。独立分支：`codex/fix-memorial-reading`。代码提交：`6ef9d2249b334926c82537874a3107be8b8f5417`。后续说明提交不修改这些被测代码字节。

本轮只修共用奏疏 UI，不改 AI、财政或批复业务规则，不改版本。原工作树与历史分支保留；**本地修复，未推送、未合并、未部署或发布**。

## 修复内容

- `phase8-formal-drafts.js` / `phase8-formal-drafts-message-panels.js`：选择下方奏疏时捕获朱批草稿，然后只替换正文和右侧详情，保留真实左栏 DOM、列表滚动位置和筛选。重选同一奏疏不重建读者区；密折启封沿用局部刷新。改筛选和完成批复仍走完整渲染，保证数量及分组正确。
- 原弹窗在关闭/重开前原有的草稿保存继续保留，未触发 input 的快捷插入也在切换前收集。复用已有 `_tmCaptureWorldLease/_tmWorldLeaseCurrent`，旧弹窗在换 GM、P、时间线或加载代际后不能把迟到输入写进新局。
- 正文改用深墨；右栏重要说明不再用过浅的文字颜色，辅助说明提高到13px，卡片背景更实。原纸卷、朱批、图层和三栏外观保留；鸿雁 stylesheet 不受本次阅读适配影响。
- `tm-theme-font.js`：原“设置 → 主题字号 → 百官奏疏”接到实际 `.zou-yuan`。保持各文本角色原字号比例，全局和分区字号各应用一次；小窗口最大字号允许中间栏滚动，正文至少保留120px，所有批阅按钮可滚到并命中，不删操作或强制缩回字体。
- 同一设置卡新增文字颜色：**深墨（默认）、墨黑（高对比）、靛青**。仅允许这些预设，不能把任意字符串注入 CSS；记录在本机设置，不写游戏世界。选择“中”/“深墨（默认）”可恢复默认。

## 验证

完整命令、退出码、日志路径、11个代码/测试/清单文件hash见 [机器证据](evidence/memorial-reading-20260910.json)。全量测试在提交前的修改树上执行，随后逐文件验证其字节与代码提交完全一致；不能把报告中的起点HEAD误读为“只测了未修改的main”。

| 检查 | 结果 |
| --- | --- |
| 新偏好设置行为 Smoke | 6组 PASS |
| 奏疏/鸿雁/字号/设置相关 Smoke | 18脚本 PASS |
| 正式全量 `node web/scripts/ci-smokes.js` | **933 PASS /0 FAIL /0 SKIP /2 WAIVED，935脚本完整执行** |
| 架构守卫 | 13 PASS |
| 官方剧本同步/对账 | 2来源/9派生物无数据变更；27 PASS |
| 发布契约 / 构建闸门夹具 | 166 /27 PASS |
| 生产依赖 audit | 0 vulnerabilities |
| 热更清单检查 | 687在场文件一致；410缺席资产条目保留 |
| 新真实 Electron 奏疏回归 | 19检查 PASS（含5项共用桥接检查） |
| 原诏书清晰度 / 营造 / 桌面保存及恢复 Electron | 15 /27 /29检查 PASS |
| 行尾、原CSS字符串、`git diff --check` | 通过；未改原行尾或中文名称 |

全量的两项豁免仍是原来的7个具体缺席资产检查，无新增豁免。测试未调用真实玩家账号/API或存档。Electron33.4.11采用独立userData和阻断外网，实际加载正式 main/preload/UI，以Electron输入事件驱动点击；不是原生OS鼠标，也不冒充安卓真机或发布后网页验收。

奏疏回归覆盖：下方条目滚动/真实节点不变、正文对应、手动/快捷插入草稿、重选/全文展开、密折、筛选、关开恢复、空列表、三栏/朱批字号、全局叠加、颜色持久化/非法值、小窗口最大字号按钮可达、换局及同对象身份变化。

## 中间失败没有隐藏

1. 修复前真实回归捕获9个未满足的新契约/功能判据，另有4个业务正常对照及5个桥接检查通过；不称为9个独立Bug。
2. 初版大字号在1280×720挤出按钮。实测几何确认后，改为阅读栏可滚动。验收从“所有按钮同时显示”调整为“保持所选字号、120px正文、滚动后每个按钮在面板内且实际可命中”；没有删除按钮或降低字号断言。
3. 首次主题筛选误把 `--grep` 当正则，发现0项被runner正确拒绝；改用其正式支持的多个 `--grep` 并集，18项完整通过。
4. 首次全量：932 PASS /1 FAIL /2 WAIVED；唯一失败是新增颜色入口后启动清单 stale。运行官方 `build-startup-phase-manifest.js` 更新清单，再生成热更清单并重跑，全量通过。未改任何测试断言或校验基线来掩盖失败。

## 派生物与边界

用官方生成器同步启动清单和热更清单。启动仍410 eager /8 deferred；热更清单仍1097项、版本1.3.4.11，只有本轮5个运行时/清单路径的hash变化。没有改官方剧本正文、美术资源、版本戳、依赖锁、发布入口或存档格式。

新CI步骤只增加奏疏真实Electron回归；尚未推送，因此不声称本轮远端CI已执行。此修复位于共用web层，但本轮实测平台为Windows Electron，安卓设备/部署后的网页仍须在对应交付阶段验证。
